# NEP 训练记录

## 力 RMSE 偏大时先查什么

- 先查 DFT 标签。SCF、K 点或截断能没收敛，力误差不会靠训练消失。
- 再查训练集覆盖。高温、高压、表面和缺陷构型不能只靠平衡体相数据外推。
- 用 RDF 检查 `r_cut` 是否至少覆盖需要的近邻壳层。
- 能量误差低但力误差高时，再调整能量和力的权重，并同时观察两项误差。

## 训练集

- 温度和体积范围要覆盖后续模拟条件。
- 应用里有表面、缺陷或相变时，训练集里也要有对应构型。
- 轨迹先去重或做 FPS，避免相邻帧占掉大部分样本。
- 单独统计少数局部环境；样本不足时定向补算。
